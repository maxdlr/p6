export interface Theme {
  id: number;
  name: string;
  description: string;
  createAt: string | null;
  updatedAt: string | null;
}

export interface ArticleTheme {
  id: number;
  name: string;
}
